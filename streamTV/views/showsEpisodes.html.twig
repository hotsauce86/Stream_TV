{# showEpisodes.html.twig
TIM TIERNEY
	This file provides the template for displaying a details for a specific EPISODE
	in the database.  It sets up the table style, and then builds the html code
	for the table of information about the toy.
#}

{% include 'header.twig' %}
<style>
table, tr, th, td { border: 0px !important; }
td, th {padding: .4em;}
.id { text-align: center; }
.name { text-align: center; }
.price { text-align: right; }
</style>


{% if results %}

<table>
	<tr>
	<th class="attr">Season</th>
	<th class="attr">title</th>
	<th class="attr">airdate</th>
	</tr>

	{% for item in results %}
	<tr>
	<td class="value">{{item.episodeID}}</td>
	<td class="value">{{item.title}}</td>
	<td class="value">{{item.airdate}}</td>
	</tr>

	{% endfor %}

<table>

{% endif %}

{% include 'footer.twig' %}
